<header class="navbar navbar-inverse navbar-fixed-top" id="app-header">
  <div class="navbar-inner">

    <div class="container">
      <%= link_to 'TicTraking', root_path, class: 'brand' %>


      <!-- <div class="pull-right"> -->
        <ul class="nav pull-right">
          <% if user_signed_in? %>
            <li class="divider-vertical"></li>
            <li class="dropdown">
              <%= link_to '#', { class: 'dropdown-toggle', 'data-toggle' => 'dropdown' } do %>
                Time worked <b class="caret"></b>
              <% end %>
              <ul class="dropdown-menu">
                 <li><%= link_to 'by me', times_worked_index_path %></li>
                 <li><%= link_to 'by my employees', times_worked_admin_path %></li>
              </ul>
            </li>
            <li class="divider-vertical"></li>
            <li class="dropdown">
              <%= link_to '#', { class: 'dropdown-toggle', 'data-toggle' => 'dropdown' } do %>
                Projects <b class="caret"></b>
              <% end %>

               <ul class="dropdown-menu">
                 <li><%= link_to 'New Project', new_project_path%></li>
                 <li class="divider"></li>
                 <li class="nav-header">Last projects</li>
                    <% @latests_projects.each do |project| %>
                      <li><%= link_to project.name, project_path(project) %></li>
                    <% end %>
                 <li class="divider"></li>
                 <li><%= link_to 'All Projects', projects_path%></li>
               </ul>
            </li>
            <li class="divider-vertical"></li>
            <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <i class="icon-cog icon-large"></i><b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li class="account-with-image">
                    <a href="<%= edit_user_registration_path %>">
                      <%= image_tag avatar_url(current_user), alt: current_user.name,
                        size: '40x40', class: 'img-rounded pull-left' %>
                      <span class="text-account">
                        <strong><%= current_user.name || current_user.email %></strong><br />
                        <small>Edit your account</small>
                      </span>
                    </a>
                </li>
                <li class="divider"></li>
                <li><%= link_to '<i class="icon-off"></i> Sign out'.html_safe,
                  destroy_user_session_path, { method: :delete } %></li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to 'Sign in', new_user_session_path %></li>
          <% end -%>
        </ul>
      <!-- </div> -->
  </div>
  </div>
</header>