<% if @task.errors.empty? %>
  $('#app-tasks').prepend("<%= escape_javascript(render 'show', task: @task, worktime: @worktime ) %>");
  $('#task_name').val('');
<% else %>
  $('.alert-error').removeClass('hide').find('span').html("Task can't be created");
<% end %>

