<% if @task.errors.empty? %>
  $('#app-tasks').prepend("<%= escape_javascript(render 'show', task: @task ) %>");
  $('#task_name').val('');
<% else %>
  $('#task_name').val('');
  appShowFlashMessage('<%= escape_javascript(flash_message) %>');
<% end %>

