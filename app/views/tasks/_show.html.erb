<div data-id="<%= task.id %>">
  <div class="row-fluid">
    <span class="lead pull-left"><%= task.name %></span>
      <div class="btn-group pull-right">
        <%= link_to '<i class="icon-play"> </i>'.html_safe, task_worktimes_path(task) , {method: :post, remote: true, class: "btn"} %>
        <% unless current_user.worktimes.where(task_id: task).empty? %>
          <%= link_to '<i class="icon-stop"> </i>'.html_safe,
           task_worktime_path(task, current_user.worktimes.last),
             {method: :put, remote: true, class: "btn"} %>
         <% end %>
      </div>
  </div>

  <ul class="app-worktimes">
    <% task.worktimes.each do |worktime|  %>
      <%= render 'worktimes/show', task: task, worktime: worktime %>
    <% end %>
  </ul>

  <% if can? :update, task %>
    <%= link_to 'Edit', edit_task_path(task) %>
  <% end %>
  <% if can? :destroy, task %>
    <%= link_to 'Delete', task_path(task), {method: :delete} %>
  <% end %>
</div>
</br>
