var $task = $('div[data-id="<%= @task.id %>"]');
var $start = $('div[data-id="<%= @task.id %>"] a[data-method="post"]');

$task.html("<%= escape_javascript(render 'tasks/show', task: @task, worktime: @worktime ) %>");