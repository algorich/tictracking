var $task = $('div[data-id="<%= @task.id %>"]');

$task.replaceWith("<%= escape_javascript(render 'tasks/show', task: @task, worktime: @worktime ) %>");
$('div[data-id="<%= @task.id.to_s %>"] a[data-method="post"]').hide();
$('div[data-id="<%= @task.id %>"] a[data-method="put"]').show();